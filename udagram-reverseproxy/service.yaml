apiVersion: v1
kind: Service
metadata:
  name: udagram-reverseproxy-svc
  labels:
    run: udagram-reverseproxy-svc
spec:
  ports:
  - port: 8080
    protocol: TCP
  selector:
    run: udagram-reverseproxy-svc